@{
    ViewBag.Title = "Syntax Verifier";
}
@model CommandAndControl.Models.FileName


<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>@ViewBag.Title</title>
        <link rel="stylesheet" href="/css/styles.css" />
        <link rel="stylesheet" href="/lib/bootstrap/dist/css/bootstrap.css" />
    </head>
    <body>
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <label>Verify Equipment Configuration File</label>
                </div>
            </div>
            <div class="row">
                <div class="col-md-6">

                    <label for="fileNames"> Select a file</label>
                    <!--<label for="file-input">-->
                    @*<div>Click this div and select a file</div>*@
                    <!--</label>-->
                    @*<input type="file" id="file-input" />*@

                    @using (Html.BeginForm("SyntaxVerifier", "Home", FormMethod.Post))
                    {
                        <div class="panel panel-success">
                            <div class="panel-body">
                                <select name="filePath" id="myFile">
                                    @foreach (var str in ViewBag.FileNames)
                                    {
                                        <option value="@Html.Raw(str) "> @Html.Raw(str)</option>
                                    }
                                </select>
                            </div>
                            <input type="submit" value="Submit">
                        </div>
                    }
                    @using (Html.BeginForm("SyntaxVerifier", "Home", FormMethod.Post))
                    {
                        <div class="panel panel-success">
                            <div class="panel-body">
                                <input name="filePath" id="myFile" type="file" />
                            </div>
                            <input type="submit" value="Submit">
                        </div>
                    }

                    <div>
                        <label>@ViewBag.FileStatus</label>
                    </div>


                </div>
                <div class="col-md-6">
                    <!-- Submit button sends to scpiverifer service-->
                    <!--</div>-->
                    <!--<div class="col-md-6">
    <h3> Equipment Configuration File Verifier</h3>
    <p> This tool verifies the configuration file selected is valid for the device</p>-->
                    <!-- Drop Down of available devices-->
                    <!-- Select File/ Browse button for user to select configuration file to compare against-->
                    <!-- Submit button-->
                </div>
            </div>
        </div>
    </body>
</html>
